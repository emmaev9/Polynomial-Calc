import java.util.List;

public class Operations {
    /*public Polynomial add(Polynomial P, Polynomial Q){
        Polynomial RES ;
        List <Monomial> result;
        List<Monomial> p1 = P.getM();
        List<Monomial> p2 = Q.getM();
        /*int size=0;
        if(p1.size() >= p2.size())
            for(int i=0;i<p1.size();i++){
                for(int j=0;j<p2.size();j++){
                    if(p1.get(i).getExp() == p2.get(j).getExp()){
                        result.get(i).setExp(i);
                        result.get(i).setCoef(p1.get(i).getCoef() + p2.get(j).getCoef());
                    }
                }
            }
        else{
            for(int i=0;i<p2.size();i++){
                for(int j=0;j<p1.size();j++){
                    if(p2.get(i).getExp() == p1.get(j).getExp()){
                        result.get(i).setExp(i);
                        result.get(i).setCoef(p1.get(i).getCoef() + p2.get(j).getCoef());
                    }
                }
            }
        }
        int i = 0;
        int j = 0;
        while(i < p1.size()){
            while( j <p2.size()){
                if(p1.get(i).getExp() == p2.get(j).getExp()){
                    //p1.get()
                }
            }
        }
       return RES;

*/
}
